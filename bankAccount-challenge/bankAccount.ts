class bankAccount {
    owner: string;
    balance: number;
    transactions: array;

    constructor(owner: string, balance: number, transactions: array) {
        this.owner = owner;
        this.balance = balance;
        this.transactions = transactions;
    }

    getBalance()

    withdrawal()

    deposit()
}